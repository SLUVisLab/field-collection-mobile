import React from 'react';
import { View } from 'react-native';

const BarcodeSetup = ({ options, setOptions }) => {

    return (
        <View>
        </View>
    );
};

export default BarcodeSetup;