import React from 'react';
import { View } from 'react-native';

const PetalCountSetup = ({ options, setOptions }) => {

    return (
        <View>
        </View>
    );
};

export default PetalCountSetup;